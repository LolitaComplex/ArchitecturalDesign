package com.doing.kt.coroutines

import java.util.*

class Test {

    fun test() {
        val scanner = Scanner(System.`in`)
        val nextLine = scanner.nextLine()

        if (nextLine.isBlank() or (nextLine.length > 1)) {

        }
        nextLine.length

        nextLine.toCharArray()[0].lowercaseChar()
        nextLine.toInt()
        val list = mutableListOf<Int>()
        list.size
        val array = IntArray(1001) { -1 }
        array.size
        val builder = StringBuilder()
        builder.clear()
    }
}